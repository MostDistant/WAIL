#!/usr/bin/env bash
# Fetch the vendored Ableton Link SDK (and its nested asio submodule) that the
# cgo binding in wail-app/internal/abllink compiles against.
set -euo pipefail
cd "$(dirname "$0")/.."
git submodule update --init --recursive vendor/link
